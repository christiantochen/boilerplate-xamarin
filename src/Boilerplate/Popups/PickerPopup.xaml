<?xml version="1.0" encoding="UTF-8" ?>
<pages:PopupPage x:Class="Boilerplate.Popups.PickerPopup"
                 xmlns="http://xamarin.com/schemas/2014/forms"
                 xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
                 xmlns:animations="clr-namespace:Rg.Plugins.Popup.Animations;assembly=Rg.Plugins.Popup"
                 xmlns:controls="clr-namespace:Boilerplate.Controls"
                 xmlns:pages="clr-namespace:Rg.Plugins.Popup.Pages;assembly=Rg.Plugins.Popup"
                 Padding="16"
                 CloseWhenBackgroundIsClicked="true">
    <pages:PopupPage.Content>
        <StackLayout Padding="16" BackgroundColor="{StaticResource WhiteColor}">
            <Label FontFamily="{StaticResource RobotoBold}"
                   FontSize="{StaticResource LargeSize}"
                   Text="{Binding Title}" />
            <ScrollView VerticalOptions="FillAndExpand">
                <StackLayout />
            </ScrollView>
        </StackLayout>
    </pages:PopupPage.Content>
</pages:PopupPage>